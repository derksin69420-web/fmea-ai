# FMEA.ai

Single-file web app for generating Failure Mode and Effects Analysis tables with OpenAI.

## Notes

- Built with OpenAI Codex
- Main app: `index.html`
- Stores the API key locally in browser `localStorage` as `fmea_openai_key`
- Includes a built-in Hydraulic Brake Assembly demo that works without an API key
